
```
# Install
    1. Download all dependencies.
            npm install
            
    2. Create mysql db (utf8)
        CREATE DATABASE node CHARACTER SET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
        
    3. Edit the file ./config.ts with your own settings:
# Start App
	npm run dev

### Production: Run app in production environment
	npm start
